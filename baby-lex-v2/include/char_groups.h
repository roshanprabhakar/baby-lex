#ifndef CHAR_GROUPS_H
#define CHAR_GROUPS_H

int is_lower(char c);
int is_upper(char c);
int is_letter(char c);
int is_digit(char c);
int is_unary(char c);
int is_binary(char c);

#endif // CHAR_GROUPS_H
